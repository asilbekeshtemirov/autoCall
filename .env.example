# =================================
# SERVER-SIDE CONFIGURATION (Never expose to client!)
# =================================

# Sipuni API Configuration
# Get your JWT token from https://lk.sipuni.com (admin account)
SIPUNI_API_BASE_URL=https://apilk.sipuni.com/api/ver2
SIPUNI_TOKEN=your_jwt_token_here

# MongoDB Configuration
# Format: mongodb://username:password@host:port/database
# Or use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/sipuni-app

# JWT Secret for user authentication
# Generate with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# =================================
# CLIENT-SIDE CONFIGURATION (Public)
# =================================

# Application Environment
NEXT_PUBLIC_APP_ENV=development

# Enable debug mode
NEXT_PUBLIC_DEBUG=false
